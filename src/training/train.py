



def train():
    # generate a dataset from the queue

    # NOTE: apply augmentation at runtime for stochastic epochs 
    # train_dataset = train_dataset.map(apply_augmentation_fn, num_parallel_calls=tf.data.AUTOTUNE)





def retrain():

    # call train() with small learning for fine tuning





